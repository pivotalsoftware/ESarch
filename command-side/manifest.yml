applications:
- name: command-side
  path: target/trader-command.jar
  timeout: 120
  instances: 1
  random-route: true
  buildpack: java_buildpack
  services:
    - mysql
    - rabbit
    - config
    - registry
