---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: "8-jdk-alpine"

inputs:
- name: source-code

outputs:
- name: package-output

run:
  path: source-code/ci/tasks/build-command-side.sh
